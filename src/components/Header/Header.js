import React from "react";

const Header = () => (
  <header className="header">
    <img src="logo192.png" className="logo" alt="logo" />
    <span className="website-title">ButtonMaker</span>
  </header>
);

export default Header;
